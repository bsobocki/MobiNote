import 'list_element.dart';

class ElementData {
  final int depth;
  final ElementType elemType;
  final int initCounterValue;
  final String initText;
  final int infoPageId;

  ElementData(this.depth, this.elemType, this.initCounterValue, this.initText, this.infoPageId);
}